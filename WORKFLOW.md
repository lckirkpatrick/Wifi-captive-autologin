# Git / PR Workflow

Branches:
- dev   (auto-approved)
- stage (auto-approved after smoke test)
- main  (manual approval only)

Rules:
- All changes via PR
- Small, focused PRs
- No rewrites; extend only
